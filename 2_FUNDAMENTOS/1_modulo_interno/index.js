const meuModulo = require('./meu_modulo')// ./ indica que esta na mesma pasta ou seja é um modulo interno.
const soma = meuModulo.soma //simplificar a sintaxe. 

var n1 = soma(2,2)
var n2 = soma(10,20)